#!/usr/bin/node
/**
 * Prints a strng to the console (STDOUT).
 * @param {strng} messge - The string to print.
 */

function displayMessage(message) {
    console.log(message);
}


module.exports = displayMessage;
