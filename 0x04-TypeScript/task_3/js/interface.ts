// task_3/js/interface.ts

// Create a type RowID equal to number
export type RowID = number;

// Create an interface RowElement with specified fields
export interface RowElement {
  firstName: string;
  lastName: string;
  age?: number;
}
